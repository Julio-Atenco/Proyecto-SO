/* Este layout hereda el sidebar de app/apuntes/layout.tsx.
   Solo añade lo que sea específico de la sección 3. */
export default function Seccion3Layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
